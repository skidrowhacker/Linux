# Linux/Bash-Summary  - #!/bin/bash echo "skidrow;Linux-Notes!"

// Linux Summary Notes  //  - Skidrow  

1. GNU vs Linux, kernel, distributions (Debian, Red Hat, Arch), shells (sh, bash), environment
variables, command line basics

2.Linux File System / , /boot, /etc, /bin, /sbin, /usr, /var, /home, /root, /proc, /sys, /dev, /tmp, /mnt, /media, /opt

3.Files and Directories pwd, ls, cd, mkdir, rm, cp, mv, find, locate, tree, file, stat

4.Text Processing cat, less, head, tail, grep, sed, awk, cut, sort, uniq, tr, wc

5.Users, Groups, and Permissions /etc/passwd, /etc/shadow, chmod, chown, chgrp, umask, ACLs, SUID, SGID, Sticky Bit

6.Process Management ps, top, htop, jobs, bg, fg, kill, pkill, killall, signals, systemd

7.Package Management APT, DPKG, RPM, DNF, YUM

8.Storage MBR vs GPT, fdisk, parted, lsblk, mount, umount, mkfs, fsck, LVM, RAID

9.Networking ip, ss, ping, dig, host, nmcli, nmtui, ssh, scp, rsync 

10.Scheduling & Automation cron, crontab, at jobs 

11.Shell Scripting Shebang, variables, loops, conditions, functions, exit codes 

12.Troubleshooting journalctl, dmesg, lspci, lsusb, smartctl, vmstat, iostat, free, uptime
